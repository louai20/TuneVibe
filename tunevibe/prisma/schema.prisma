datasource db {
  provider = "mongodb"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id       String    @id @default(auto()) @map("_id") @db.ObjectId
  name     String
  email    String    @unique
  password String
}


model Playlist {
  id       String   @id @default(auto()) @map("_id") @db.ObjectId
  name     String
  url      String   // Store the external Spotify URL
  createdAt DateTime @default(now())
}